const express = require("express");
const route = express.Router();
app.set("view engine", "pug");

const express = require("express");
const route = express.Router();
app.set("view engine", "pug");

app.get("/", getBook);

module.exports = route;
